# PR #296: updates makechangelog bot

- **Author:** AMcilraith
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/296

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

<!-- Describe The Pull Request. -->

<hr>
</details>

## Changelog
```
:cl:
del: Removed old make changelog bot setup  
add: Added new changelog generation process using updated tools
/:cl:
```

**Changelog entries (included in build):**

- **add:** Added new changelog generation process using updated tools

<details>
<summary>Files changed</summary>

- `.github/workflows/make_changelog.yml` (+18 / -25) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/.github/workflows/make_changelog.yml b/.github/workflows/make_changelog.yml
index f11d417c232..7451aa00863 100644
--- a/.github/workflows/make_changelog.yml
+++ b/.github/workflows/make_changelog.yml
@@ -1,37 +1,30 @@
-name: Make changelogs
+name: Generate Changelog
 
 on:
   push:
-    branches: [master]
+    branches:
+      - main
+      - changelog-fixes
 
 jobs:
-  MakeCL:
+  generate-changelog:
     runs-on: ubuntu-latest
+
     steps:
-      - name: Checkout
-        uses: actions/checkout@v1
-        with:
-          fetch-depth: 25
-      - name: Python setup
-        uses: actions/setup-python@v1
+      - name: Checkout code
+        uses: actions/checkout@v3
+
+      - name: Set up Python
+        uses: actions/setup-python@v4
         with:
-          python-version: '3.x'
-      - name: Install depends
-        run: |
-          python -m pip install --upgrade pip
-          pip install ruamel.yaml PyGithub
-          pip install PyYAML
-      - name: Make CL
+          python-version: 3.10
+
+      - name: Install dependencies
+        run: pip install -r requirements.txt
+
+      - name: Run changelog generator
         env:
-          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
-          GIT_EMAIL: "action@github.com"
-          GIT_NAME: "Nestor Jr."
+          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
         run: python tools/generate_cl.py
-      - name: Compile CL
-        env:
-          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
-          GIT_EMAIL: "action@github.com"
-          GIT_NAME: "Nestor Jr."
-        run: python tools/changelog/ss13_genchangelog.py "html/changelogs"
 
 

```
</details>
