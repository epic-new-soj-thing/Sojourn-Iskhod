# PR #110: Sink Pressure Removal

- **Author:** ryuu-dragon
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/110

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

Removes sink pressure and related vars, at the request of various server members.
<hr>
</details>

## Changelog
```
:cl:
del: Removed sink pressure requirements for washing hands.  
del: Sinks no longer have limited reagents or refill rates.
/:cl:
```

**Changelog entries (included in build):**

- **del:** Sinks no longer have limited reagents or refill rates.

<details>
<summary>Files changed</summary>

- `code/game/objects/effects/decals/crayon.dm` (+3 / -3) modified
- `code/game/objects/structures/watercloset.dm` (+41 / -37) modified
- `maps/__Nadezhda/map/_Nadezhda_Colony.dmm` (+0 / -1) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/game/objects/effects/decals/crayon.dm b/code/game/objects/effects/decals/crayon.dm
index 7bac49b92a0..4cb71a7c618 100644
--- a/code/game/objects/effects/decals/crayon.dm
+++ b/code/game/objects/effects/decals/crayon.dm
@@ -1765,9 +1765,9 @@ obj/item/scroll/proc/example_spell(mob/living/carbon/human/M) //testing spell
 			An inkwell of tea in which to dip your fingers to write in tea."
 	icon_state = "blood_basin"
 	density = 1
-	limited_reagents = FALSE
-	refill_rate = 200
-	reagent_id = "blood"
+//	limited_reagents = FALSE
+//	refill_rate = 200
+//	reagent_id = "blood"
 
 /obj/structure/sink/basion/crayon/attack_hand(mob/living/carbon/human/user) //gives us bloody hands for writing spells.
 	if(istype(user))
diff --git a/code/game/objects/structures/watercloset.dm b/code/game/objects/structures/watercloset.dm
index 61b80c4ab22..2a1385eb22b 100644
--- a/code/game/objects/structures/watercloset.dm
+++ b/code/game/objects/structures/watercloset.dm
@@ -424,21 +424,21 @@
 	anchored = 1
 	reagent_flags = OPENCONTAINER
 	var/busy = 0 	//Something's being washed at the moment
-	var/reagent_id = "water"
+//	var/reagent_id = "water"
 
-//Inspired by TG plumming
-	var/limited_reagents = TRUE
-	var/amount_of_reagents = 200
-	var/max_amount = 200
-	var/refill_rate = 0.5
+// Inspired by TG plumming
+//	var/limited_reagents = TRUE
+//	var/amount_of_reagents = 200
+//	var/max_amount = 200
+//	var/refill_rate = 0.5
 
-/obj/structure/sink/New()
-	..()
-	START_PROCESSING(SSobj, src)
+///obj/structure/sink/New()
+//	..()
+//	START_PROCESSING(SSobj, src)
 
-/obj/structure/sink/Process()
-	if(max_amount >= amount_of_reagents)
-		amount_of_reagents += refill_rate
+///obj/structure/sink/Process()
+//	if(max_amount >= amount_of_reagents)
+//		amount_of_reagents += refill_rate
 
 /obj/structure/sink/MouseDrop_T(var/obj/item/thing, var/mob/user)
 	. = ..()
@@ -469,9 +469,9 @@
 	if(!Adjacent(user))
 		return
 
-	if(amount_of_reagents < 40 && limited_reagents)
-		to_chat(user, SPAN_WARNING("The water pressure seems too low to wash with."))
-		return
+//	if(amount_of_reagents < 40 && limited_reagents)
+//		to_chat(user, SPAN_WARNING("The water pressure seems too low to wash with."))
+//		return
 
 	if(busy)
 		to_chat(user, SPAN_WARNING("Someone's already washing here."))
@@ -489,7 +489,7 @@
 		return
 	busy = FALSE
 
-	amount_of_reagents -= 40
+//	amount_of_reagents -= 40
 	user.clean_blood()
 	if(ishuman(user))
 		var/mob/living/carbon/human/H = user
@@ -511,18 +511,22 @@
 
 	var/obj/item/reagent_containers/RG = O
 	if (istype(RG) && RG.is_refillable())
-		var/amount_to_add = min(RG.volume - RG.reagents.total_volume, RG.amount_per_transfer_from_this)
-		if(amount_to_add > amount_of_reagents)
-			amount_to_add = amount_of_reagents
-		if(amount_of_reagents)
-			RG.reagents.add_reagent(reagent_id, amount_to_add)
-			user.visible_message(SPAN_NOTICE("[user] fills \the [RG] using \the [src]."),SPAN_NOTICE("You fill \the [RG] using \the [src]."))
-			playsound(loc, 'sound/effects/watersplash.ogg', 100, 1)
-			if(limited_reagents)
-				amount_of_reagents -= amount_to_add
-		else
-			to_chat(user, SPAN_WARNING("The sink seems to be out of pressure"))
+		RG.reagents.add_reagent("water", min(RG.volume - RG.reagents.total_volume, RG.amount_per_transfer_from_this))
+		user.visible_message(SPAN_NOTICE("[user] fills \the [RG] using \the [src]."),SPAN_NOTICE("You fill \the [RG] using \the [src]."))
+		playsound(loc, 'sound/effects/watersplash.ogg', 100, 1)
 		return 1
+//		var/amount_to_add = min(RG.volume - RG.reagents.total_volume, RG.amount_per_transfer_from_this)
+//		if(amount_to_add > amount_of_reagents)
+//			amount_to_add = amount_of_reagents
+//		if(amount_of_reagents)
+//			RG.reagents.add_reagent(reagent_id, amount_to_add)
+//			user.visible_message(SPAN_NOTICE("[user] fills \the [RG] using \the [src]."),SPAN_NOTICE("You fill \the [RG] using \the [src]."))
+//			playsound(loc, 'sound/effects/watersplash.ogg', 100, 1)
+//			if(limited_reagents)
+//				amount_of_reagents -= amount_to_add
+//		else
+//			to_chat(user, SPAN_WARNING("The sink seems to be out of pressure"))
+//		return 1
 
 	else if (istype(O, /obj/item/tool/baton))
 		var/obj/item/tool/baton/B = O
@@ -557,9 +561,9 @@
 		SPAN_NOTICE("You start washing [I] using [src].")
 	)
 
-	if(amount_of_reagents < 40)
-		to_chat(user, SPAN_WARNING("The water pressure seems too low to wash with."))
-		return
+//	if(amount_of_reagents < 40)
+//		to_chat(user, SPAN_WARNING("The water pressure seems too low to wash with."))
+//		return
 
 	busy = TRUE
 	if(!do_after(user, 40, src))
@@ -567,8 +571,8 @@
 		return
 	busy = FALSE
 
-	if(limited_reagents)
-		amount_of_reagents -= 40
+//	if(limited_reagents)
+//		amount_of_reagents -= 40
 
 	if(QDELETED(I))
 		return 								//Item's been destroyed while washing
@@ -595,14 +599,14 @@
 	name = "water fountain"
 	desc = "A small fountain of polished stone that has a small pump at the bottom which helps pump out fresh water."
 	icon_state = "BaptismFont_Water"
-	refill_rate = 2
+//	refill_rate = 2
 
 /obj/structure/sink/basion
 	name = "water basin"
 	desc = "A deep basin of polished stone that has been pre-filled with fresh water."
 	icon_state = "BaptismFont_Water"
-	limited_reagents = FALSE
-	refill_rate = 200
+//	limited_reagents = FALSE
+//	refill_rate = 200
 
 /obj/structure/sink/kitchen
 	name = "kitchen sink"
@@ -613,8 +617,8 @@
 	name = "puddle"
 	desc = "A puddle of still water. Great for splashing others."
 	icon_state = "puddle"
-	limited_reagents = FALSE
-	refill_rate = 200
+//	limited_reagents = FALSE
+//	refill_rate = 200
 
 /obj/structure/sink/puddle/attack_hand(mob/M as mob)
 	icon_state = "puddle-splash"
diff --git a/maps/__Nadezhda/map/_Nadezhda_Colony.dmm b/maps/__Nadezhda/map/_Nadezhda_Colony.dmm
index 093d24b498e..5d31a9ee71b 100644
--- a/maps/__Nadezhda/map/_Nadezhda_Colony.dmm
+++ b/maps/__Nadezhda/map/_Nadezhda_Colony.dmm
@@ -128207,7 +128207,6 @@
 /area/nadezhda/maintenance/undergroundfloor1north)
 "yaE" = (
 /obj/structure/sink/fountain{
-	limited_reagents = 0;
 	name = "high flow water fountain"
 	},
 /turf/simulated/floor/asteroid/dirt/dust,

```
</details>
