# PR #188: admin-only-who

- **Author:** AMcilraith
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/188

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

<!-- Describe The Pull Request. -->

<hr>
</details>

## Changelog
```
:cl:
tweak: The 'who' command is now restricted to administrators only if enabled in configuration.
/:cl:
```

**Changelog entries (included in build):**

- **tweak:** The 'who' command is now restricted to administrators only if enabled in configuration.

<details>
<summary>Files changed</summary>

- `code/controllers/configuration.dm` (+4 / -0) modified
- `code/game/verbs/who.dm` (+6 / -1) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/controllers/configuration.dm b/code/controllers/configuration.dm
index ddaaec6d9e1..e5276042ae4 100755
--- a/code/controllers/configuration.dm
+++ b/code/controllers/configuration.dm
@@ -65,6 +65,7 @@ GLOBAL_LIST_EMPTY(storyteller_cache)
 	var/kick_inactive = 0				//force disconnect for inactive players after this many minutes, if non-0
 	var/show_mods = 1
 	var/show_mentors = 0
+	var/admin_only_who = 0				//restrict who command to admins only
 	var/mods_can_tempban = 0
 	var/mods_can_job_tempban = 0
 	var/mod_tempban_max = 1440
@@ -552,6 +553,9 @@ GLOBAL_LIST_EMPTY(storyteller_cache)
 				if("show_mentors")
 					config.show_mentors = 1
 
+				if("admin_only_who")
+					config.admin_only_who = 1
+
 				if("mods_can_tempban")
 					config.mods_can_tempban = 1
 
diff --git a/code/game/verbs/who.dm b/code/game/verbs/who.dm
index 631f6751275..91b09c7906f 100644
--- a/code/game/verbs/who.dm
+++ b/code/game/verbs/who.dm
@@ -2,6 +2,11 @@
 	set name = "Who"
 	set category = "OOC"
 
+	// Check if who command is restricted to admins only
+	if(config.admin_only_who && (!holder || !(R_ADMIN & holder.rights || R_MOD & holder.rights)))
+		to_chat(src, "<span class='warning'>The who command is currently restricted to administrators only.</span>")
+		return
+
 	var/msg = "<b>Current Players:</b>\n"
 
 	var/list/Lines = list()
@@ -145,4 +150,4 @@
 	if(config.show_mentors)
 		msg += "\n<b> Current Mentors ([num_mentors_online]):</b>\n" + mentmsg
 
-	to_chat(src, msg) 
+	to_chat(src, msg)

```
</details>
