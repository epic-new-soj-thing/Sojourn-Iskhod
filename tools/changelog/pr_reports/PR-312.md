# PR #312: Makes science goggle useful

- **Author:** AMcilraith
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/312

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

<!-- Describe The Pull Request. -->

<hr>
</details>

## Changelog
```
:cl:
add: Science goggles now provide access to inspect reagents in containers when active.  
add: New science goggles are now available in the medical secure closets.  
tweak: Descriptions of certain labcoats have been updated for clarity.  
fix: The R&D console can now research multiple technologies at once.
/:cl:
```

**Changelog entries (included in build):**

- **add:** New science goggles are now available in the medical secure closets.
- **tweak:** Descriptions of certain labcoats have been updated for clarity.
- **fix:** The R&D console can now research multiple technologies at once.

<details>
<summary>Files changed</summary>

- `code/datums/autolathe/autolathe_datums.dm` (+5 / -0) modified
- `code/game/atoms.dm` (+25 / -2) modified
- `code/game/objects/items/weapons/storage/boxes.dm` (+1 / -1) modified
- `code/game/objects/structures/crates_lockers/closets/secure/medical.dm` (+8 / -0) modified
- `code/modules/clothing/glasses/powered.dm` (+17 / -0) modified
- `code/modules/clothing/suits/labcoat.dm` (+4 / -4) modified
- `code/modules/research/rdconsole.dm` (+15 / -2) modified
- `nano/templates/rdconsole.tmpl` (+22 / -16) modified
- `tools/requirements.txt` (+5 / -5) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/datums/autolathe/autolathe_datums.dm b/code/datums/autolathe/autolathe_datums.dm
index 61fe5598f0a..e56813e578b 100755
--- a/code/datums/autolathe/autolathe_datums.dm
+++ b/code/datums/autolathe/autolathe_datums.dm
@@ -143,6 +143,11 @@
 		"ignore_object_materials" = ignore_object_materials
 	)
 
+	if(build_path)
+		// We use a separate field for the base64 image to avoid conflict with TGUI's icon class
+		// TGUI uses 'icon' which is populated by the design_icons spritesheet
+		nano_ui_data["img_tag"] = icon2base64tgui(build_path)
+
 	if(length(materials))
 		var/list/RS = list()
 
diff --git a/code/game/atoms.dm b/code/game/atoms.dm
index 5fe4e77f69e..92a400d5e08 100755
--- a/code/game/atoms.dm
+++ b/code/game/atoms.dm
@@ -396,14 +396,26 @@ its easier to just keep the beam vertical.
 			user.client.statpanel = "Examine"
 
 	if(reagents)
-		if(reagent_flags & TRANSPARENT)
+		var/can_see_reagents_inside = (reagent_flags & TRANSPARENT) || user.can_see_reagents()
+		if(can_see_reagents_inside)
 			to_chat(user, SPAN_NOTICE("It contains:"))
 			var/return_value = user.can_see_reagents()
 			var/cop_vision = user.can_see_illegal_reagents()
 			var/bar_vision = user.can_see_common_reagents()
+
+			var/has_sirc_access = FALSE
+			if(ishuman(user))
+				var/mob/living/carbon/human/H = user
+				if(istype(H.glasses, /obj/item/clothing/glasses/powered/science))
+					var/obj/item/clothing/glasses/powered/science/S = H.glasses
+					if(S.active) has_sirc_access = TRUE
+
 			if(return_value == TRUE) //Show each individual reagent
 				for(var/datum/reagent/R in reagents.reagent_list)
-					to_chat(user, SPAN_NOTICE("[R.volume] units of [R.name]"))
+					var/display_name = R.name
+					if(has_sirc_access)
+						display_name = "<a href='?src=[REF(src)];action=sirc_inspect;reagent=[R.type]'>[R.name]</a>"
+					to_chat(user, SPAN_NOTICE("[R.volume] units of [display_name]"))
 			// Only display reagents marked as "Common", IE, a regular person will know what it is and does.
 			else if(bar_vision)
 				var/misc_reagent = 0
@@ -453,6 +465,17 @@ its easier to just keep the beam vertical.
 
 	return distance == -1 || (get_dist(src, user) <= distance) || isobserver(user)
 
+/atom/Topic(href, href_list)
+	..()
+	if(href_list["action"] == "sirc_inspect")
+		if(ishuman(usr))
+			var/mob/living/carbon/human/H = usr // usr required for Topic hrefs
+			if(istype(H.glasses, /obj/item/clothing/glasses/powered/science))
+				var/obj/item/clothing/glasses/powered/science/S = H.glasses
+				if(S.active)
+					var/reagent_path = text2path(href_list["reagent"])
+					S.open_sirc(usr, reagent_path)
+
 // called by mobs when e.g. having the atom as their machine, pulledby, loc (AKA mob being inside the atom) or buckled var set.
 // see code/modules/mob/mob_movement.dm for more.
 /atom/proc/relaymove()
diff --git a/code/game/objects/items/weapons/storage/boxes.dm b/code/game/objects/items/weapons/storage/boxes.dm
index c5824b021e6..7c0f7af0f47 100644
--- a/code/game/objects/items/weapons/storage/boxes.dm
+++ b/code/game/objects/items/weapons/storage/boxes.dm
@@ -683,7 +683,7 @@
 
 /obj/item/storage/box/autoinjectors/populate_contents()
 	for(var/i in 1 to 7)
-		new /obj/item/reagent_containers/hypospray/autoinjector/sugar(src)
+		new /obj/item/reagent_containers/hypospray/autoinjector(src)
 
 /obj/item/storage/box/lights
 	name = "box of replacement bulbs"
diff --git a/code/game/objects/structures/crates_lockers/closets/secure/medical.dm b/code/game/objects/structures/crates_lockers/closets/secure/medical.dm
index 46e58c8d8e2..00482869b76 100644
--- a/code/game/objects/structures/crates_lockers/closets/secure/medical.dm
+++ b/code/game/objects/structures/crates_lockers/closets/secure/medical.dm
@@ -19,6 +19,10 @@
 	new /obj/item/reagent_containers/glass/bottle/antitoxin(src)
 	new /obj/item/reagent_containers/glass/bottle/antitoxin(src)
 	new /obj/item/storage/hcases/med(src)
+	new /obj/item/storage/box/pillbottles(src)
+	new /obj/item/storage/box/pillbottles(src)
+	new /obj/item/clothing/glasses/powered/science(src)
+	new /obj/item/clothing/suit/storage/toggle/labcoat/chemist(src)
 
 /obj/structure/closet/secure_closet/anesthetics
 	name = "anesthetics closet"
@@ -249,3 +253,7 @@
 	new /obj/item/storage/pouch/tubular/vial(src)
 	new /obj/item/storage/bag/chemistry(src)
 	new /obj/item/storage/bag/chemistry(src)
+	new /obj/item/clothing/glasses/powered/science(src)
+	new /obj/item/clothing/glasses/powered/science(src)
+	new /obj/item/clothing/suit/storage/toggle/labcoat/chemist(src)
+	new /obj/item/clothing/suit/storage/toggle/labcoat/chemist(src)
diff --git a/code/modules/clothing/glasses/powered.dm b/code/modules/clothing/glasses/powered.dm
index 8d4667a44f4..f451adfa53d 100644
--- a/code/modules/clothing/glasses/powered.dm
+++ b/code/modules/clothing/glasses/powered.dm
@@ -52,9 +52,26 @@
 	desc = "A pair of goggles with a science HUD. These can show you reagents within transparent containers and organoid information."
 	icon_state = "purple"
 	item_state = "glasses"
+	prescription = 1
 
 	tick_cost = 0.1
 
+	var/datum/tgui_module/catalog/chemistry/sirc
+
+/obj/item/clothing/glasses/powered/science/Destroy()
+	if(sirc)
+		QDEL_NULL(sirc)
+	. = ..()
+
+/obj/item/clothing/glasses/powered/science/proc/open_sirc(mob/user, reagent_path)
+	if(!sirc)
+		sirc = new(src)
+
+	var/datum/catalog_entry/E = GLOB.all_catalog_entries_by_type[reagent_path]
+	if(E)
+		sirc.set_selected_entry(user, E)
+		sirc.ui_interact(user)
+
 /obj/item/clothing/glasses/powered/science/Initialize()
 	. = ..()
 	screenOverlay = global_hud.science
diff --git a/code/modules/clothing/suits/labcoat.dm b/code/modules/clothing/suits/labcoat.dm
index da89da695c5..445f17f6224 100644
--- a/code/modules/clothing/suits/labcoat.dm
+++ b/code/modules/clothing/suits/labcoat.dm
@@ -44,10 +44,10 @@
 
 /obj/item/clothing/suit/storage/toggle/labcoat/virologist
 	name = "soteria virologist labcoat"
-	desc = "A suit that protects against minor chemical spills. Offers slightly more protection against biohazards than the standard model. Has a green stripe on the shoulder."
-	icon_state = "labcoat_medspec_open"
-	icon_open = "labcoat_medspec_open"
-	icon_closed = "labcoat_medspec"
+	desc = "A suit that protects against minor chemical spills. Offers slightly more protection against biohazards than the standard model. Has a blue stripe on the shoulder."
+	icon_state = "labcoat_gen_open"
+	icon_open = "labcoat_gen_open"
+	icon_closed = "labcoat_gen"
 	icon_drape = null
 	armor_list = list(
 		melee = 0,
diff --git a/code/modules/research/rdconsole.dm b/code/modules/research/rdconsole.dm
index d502ec85d2d..753fded78e5 100644
--- a/code/modules/research/rdconsole.dm
+++ b/code/modules/research/rdconsole.dm
@@ -180,6 +180,19 @@ won't update every console in existence) but it's more of a hassle to do. Also,
 		if(tech_tree && tech_tree.shown)
 			selected_tech_tree = tech_tree
 			selected_technology = null
+	if(href_list["research_all"]) // Research everything feasible
+		var/can_continue = TRUE
+		var/max_iterations = 50 // Fail-safe to prevent infinite loops, though unlikely
+		while(can_continue && max_iterations > 0)
+			can_continue = FALSE
+			max_iterations--
+			for(var/t in SSresearch.all_tech_nodes)
+				var/datum/technology/tech_node = t
+				if(!files.IsResearched(tech_node) && files.CanResearch(tech_node))
+					files.UnlockTechology(tech_node)
+					can_continue = TRUE
+		SSnano.update_uis(src)
+
 	if(href_list["select_technology"]) // User selected a technology node.
 		var/tech_node = locate(href_list["select_technology"]) in SSresearch.all_tech_nodes
 		if(tech_node)
@@ -309,9 +322,9 @@ won't update every console in existence) but it's more of a hassle to do. Also,
 				linked_imprinter = null
 	if(href_list["reset"]) //Reset the R&D console's database.
 		griefProtection()
-		var/choice = alert("R&D Console Database Reset", "Are you sure you want to reset the R&D console's database? Data lost cannot be recovered.", "Continue", "Cancel")
+		var/choice = alert(usr, "Are you sure you want to reset the R&D console's database? Data lost cannot be recovered.", "R&D Console Database Reset", "Continue", "Cancel")
 		if(choice == "Continue")
-			log_and_message_admins("reset the R&D console's database")
+			log_and_message_admins("[key_name(usr)] reset the R&D console's database.")
 			screen = SCREEN_WORKING
 			qdel(files)
 			files = new /datum/research(src)
diff --git a/nano/templates/rdconsole.tmpl b/nano/templates/rdconsole.tmpl
index 9ef9c02bda6..74deae45f79 100644
--- a/nano/templates/rdconsole.tmpl
+++ b/nano/templates/rdconsole.tmpl
@@ -31,7 +31,7 @@
 			<div class='item'>
 				{{:helper.link("Circuit Construction Menu", 'video', {'go_screen' : 'circuit_imprinter'}, data.has_circuit_imprinter ? null : 'disabled', data.has_circuit_imprinter ? 'sciMenuButton' : null)}}
 			</div>
-			
+
 			<div class="statusDisplayRecords">
 				<div class="item">
 					<div class="itemContent" style="width: 100%;">
@@ -59,7 +59,7 @@
 					</div>
 				</div>
 			</div>
-			
+
 			<div class='item'>
 				{{:helper.link("Settings", 'wrench', {'toggle_settings' : 1},null,'sciMenuButton')}}
 			</div>
@@ -77,6 +77,9 @@
 				<div class='item'>
 					{{:helper.link("Reset R&D Database", 'trash', {'reset' : 1})}}
 				</div>
+				<div class='item'>
+					{{:helper.link("Lock Console", 'locked', {'lock' : 1})}}
+				</div>
 			{{/if}}
 
 			{{if data.has_disk}}
@@ -90,11 +93,11 @@
 			{{else}}
 				No disk detected
 			{{/if}}
-			
+
 			<div class='item'>
 				{{:helper.link("Device Linkage Menu", 'link', {'toggle_link_menu' : 1},null,'sciMenuButton')}}
 			</div>
-			
+
 			{{if data.show_link_menu}}
 				<div class='item'>
 					{{:helper.link("Re-sync with Nearby Devices", 'link', {'find_device' : 1})}}
@@ -135,7 +138,7 @@
 	<div class='item'>
 		{{:helper.link("Back", 'arrowreturn-1-w', {'go_screen' : 'main'},null,'fixedLeft')}}
 	</div>
-	
+
 	<table class="fixed" style="width: 100%">
 		<tr>
 			<td valign="top">
@@ -164,8 +167,10 @@
 								<table class="fixed" style="width: 100%;">
 									{{for data.possible_designs}}
 										<tr style="width: 100%;">
-											<td style="width: 32px">
-												<img class="statusDisplayItem" src={{:value.data.icon}} height=24 width=24></img>
+											<td style="width: 38px">
+												<div style="width: 32px; height: 32px; background-color: black; border: 1px solid #3e6189; display: flex; align-items: center; justify-content: center; overflow: hidden;">
+													<img src="{{:value.data.img_tag}}" style="max-width: 100%; max-height: 100%;">
+												</div>
 											</td>
 											<td style="width: 100%">
 												{{:helper.link(value.data.name, '', {'build' : value.id, 'amount': 1}, value.can_create >= 1 ? null : 'disabled')}}
@@ -182,17 +187,17 @@
 												{{for value.data.materials  :material:material_i}}
 													{{material.amount = value.data.adjust_materials ? Math.round((material.req * data.materials_data.mat_efficiency) * 100) / 100 : material.req;}}
 													{{if value.missing_materials.indexOf(material.id) == -1}}
-														{{:material.amount}} {{:material.name}} 
+														{{:material.amount}} {{:material.name}}
 													{{else}}
-														<span style="color:red">{{:material.amount}} {{:material.name}}</span> 
+														<span style="color:red">{{:material.amount}} {{:material.name}}</span>
 													{{/if}}
 												{{/for}}
 
 												{{for value.data.chemicals  :chemical:chemical_i}}
 													{{if value.missing_chemicals.indexOf(chemical.id) == -1}}
-														{{:chemical.req}} {{:chemical.name}} 
+														{{:chemical.req}} {{:chemical.name}}
 													{{else}}
-														<span style="color:red">{{:chemical.req}} {{:chemical.name}}</span> 
+														<span style="color:red">{{:chemical.req}} {{:chemical.name}}</span>
 													{{/if}}
 												{{/for}}
 
@@ -217,11 +222,11 @@
 					<div class="item">
 						<div class="itemContent" style="width: 100%;">
 							<table class="fixed" style="width: 100%">
-				
+
 							{{for data.materials_data.reagents}}
 							<tr><td><span class="average">{{:value.name}}</span></td><td>{{:Math.round(value.amount * 100) / 100}}</td></tr>
 							{{/for}}
-							
+
 							{{for data.materials_data.materials}}
 							<tr>
 								<td><span class="average">{{:value.name.toTitleCase()}}</span></td>
@@ -235,7 +240,7 @@
 								</td>
 							</tr>
 							{{/for}}
-				
+
 							{{if (!data.materials_data.materials || !data.materials_data.materials.length) && (!data.materials_data.reagents || !data.materials_data.reagents.length)}}
 								Empty
 							{{/if}}
@@ -279,6 +284,7 @@
 	{{:helper.link("Back", 'arrowreturn-1-w', {'go_screen' : 'main'},null,'fixedLeft')}}
 
 	<span class="average">Research points:</span> {{:data.research_points}} <span class="average">{{:data.tech_tree_name}} level:</span> {{:data.tech_tree_level}}
+	{{:helper.link("Research All", 'search', {'research_all' : 1}, data.can_research ? null : 'disabled')}}
 	</div>
 	<div class='item'>
 
@@ -294,7 +300,7 @@
 				{{for data.lines}}
 					<div style="position: absolute;border-{{:value.istop?'top':'bottom'}}: 1px solid white;border-{{:value.isright?'right':'left'}}: 1px solid white;width: {{:value.width}}%;height: {{:value.height}}%;left: {{:value.line_x}}%;bottom: {{:value.line_y}}%"></div>
 				{{/for}}
-				
+
 				{{for data.techs}}
 					<div style="position: absolute; left: {{:value.x}}%; bottom: {{:value.y}}%;margin-left: -20px;margin-bottom: -20px;">
 						{{:helper.link("<div class='sciTechTreeIcon icon-"+value.icon+"'><span><img align='center' class='sciTechTreeIconToolTip icon-"+value.icon+"'><br><b>"+value.name+"</b><hr>"+value.description+"</span></div>", '', {'select_technology' : value.id}, null, "link sciTechTreeIconBox "+" "+((value.canresearch==0 && value.isresearched!=1)?'sciTechCantResearch':'')+" "+((value.isresearched==1)?'sciTechResearched':'')+" "+((value.id == data.selected_technology_id)?'sciTechSelected':'') )}}
@@ -446,4 +452,4 @@
 		{{/for}}
 		</div>
 	{{/if}}
-{{/if}}
\ No newline at end of file
+{{/if}}
diff --git a/tools/requirements.txt b/tools/requirements.txt
index ff7d929d53c..5679dd3dede 100644
--- a/tools/requirements.txt
+++ b/tools/requirements.txt
@@ -1,7 +1,7 @@
-pygit2==1.7.2
-bidict==0.22.0
-Pillow==10.3.0
+pygit2
+bidict
+Pillow
 
 # changelogs
-PyYaml==6.0.1
-beautifulsoup4==4.9.3
+PyYaml
+beautifulsoup4

```
</details>
