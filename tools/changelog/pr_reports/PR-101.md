# PR #101: Many Runtime Fixes

- **Author:** ryuu-dragon
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/101

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

Fixes for various runtimes. The sanity checks should work but I'm not sure on some of the list index runtimes. Some of these (farmbot.dm and turbine.dm especially) need to be merged as they create enough runtimes to cause server lag.

Thanks to CFWMagic for helping me find the issue in scores.dm
<hr>
</details>

## Changelog
```
:cl:
fix: Fixed various runtime errors throughout the game to improve stability and user experience.
/:cl:
```

**Changelog entries (included in build):**

- **fix:** Fixed various runtime errors throughout the game to improve stability and user experience.

<details>
<summary>Files changed</summary>

- `code/__HELPERS/text.dm` (+2 / -2) modified
- `code/game/antagonist/_antagonist_setup.dm` (+1 / -1) modified
- `code/game/gamemodes/scores.dm` (+2 / -2) modified
- `code/game/machinery/doors/airlock.dm` (+1 / -1) modified
- `code/game/objects/items/devices/organ_module/simple.dm` (+1 / -1) modified
- `code/game/objects/items/devices/transfer_valve.dm` (+4 / -1) modified
- `code/modules/client/preference_setup/preference_setup.dm` (+1 / -1) modified
- `code/modules/core_implant/cruciform/rituals/construction.dm` (+1 / -1) modified
- `code/modules/hivemind/self_defense.dm` (+2 / -1) modified
- `code/modules/mob/living/bot/farmbot.dm` (+6 / -5) modified
- `code/modules/mob/living/carbon/superior_animal/giant_spider/types/hunter/pepper.dm` (+1 / -1) modified
- `code/modules/mob/living/carbon/superior_animal/superior_animal.dm` (+1 / -1) modified
- `code/modules/mob/living/simple_animal/hostile/carp.dm` (+1 / -1) modified
- `code/modules/mob/living/simple_animal/simple_animal.dm` (+1 / -1) modified
- `code/modules/mob/mob.dm` (+1 / -1) modified
- `code/modules/organs/wound.dm` (+1 / -1) modified
- `code/modules/power/turbine.dm` (+2 / -2) modified
- `code/modules/projectiles/gun.dm` (+4 / -1) modified
- `code/modules/reagents/reagents.dm` (+6 / -4) modified
- `code/modules/synthesized_instruments/song.dm` (+2 / -2) modified
- `code/modules/tgui/tgui.dm` (+2 / -2) modified
- `code/modules/turbolift/turbolift_console.dm` (+1 / -1) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/__HELPERS/text.dm b/code/__HELPERS/text.dm
index 5ac2c34351d..1b82dd022bf 100755
--- a/code/__HELPERS/text.dm
+++ b/code/__HELPERS/text.dm
@@ -562,8 +562,8 @@ proc/TextPreview(var/string, var/len=40)
 		return ""
 	var/turf/T = get_turf(target)
 	var/area/A = get_area(target)
-	var/where = "[A? A.name : "Unknown Location"] | [T.x], [T.y], [T.z]"
-	var/whereLink = "<A HREF='?_src_=holder;adminplayerobservecoodjump=1;X=[T.x];Y=[T.y];Z=[T.z]'>[where]</a>"
+	var/where = "[A? A.name : "Unknown Location"] | [T?.x], [T?.y], [T?.z]"
+	var/whereLink = "<A HREF='?_src_=holder;adminplayerobservecoodjump=1;X=[T?.x];Y=[T?.y];Z=[T?.z]'>[where]</a>"
 	return whereLink
 
 //Used for applying byonds text macros to strings that are loaded at runtime
diff --git a/code/game/antagonist/_antagonist_setup.dm b/code/game/antagonist/_antagonist_setup.dm
index 0a3a80c8883..0cb38bb03f5 100755
--- a/code/game/antagonist/_antagonist_setup.dm
+++ b/code/game/antagonist/_antagonist_setup.dm
@@ -34,7 +34,7 @@ GLOBAL_LIST_EMPTY(faction_types)
 				return antag
 
 /proc/clear_antagonist(var/datum/mind/player)
-	for(var/datum/antagonist/A in player.antagonist)
+	for(var/datum/antagonist/A in player?.antagonist)
 		A.remove_antagonist()
 
 /proc/clear_antagonist_type(datum/mind/player, a_id)
diff --git a/code/game/gamemodes/scores.dm b/code/game/gamemodes/scores.dm
index 8da5d7b1b39..58c77e1e31d 100644
--- a/code/game/gamemodes/scores.dm
+++ b/code/game/gamemodes/scores.dm
@@ -351,8 +351,8 @@ GLOBAL_VAR_INIT(score_technomancer_faction_item_loss, 0)
 	var/max_personal_score = 1000
 
 	dat += {"
-	<b>Personal Objectives completed:</b> [mind.individual_objectives_completed] ([to_score_color(objectives_score)] Points)<br>
-	<b>Antagonist contracts completed:</b> [mind.contracts_completed] ([to_score_color(contracts_score)] Points)<br>
+	<b>Personal Objectives completed:</b> [mind?.individual_objectives_completed] ([to_score_color(objectives_score)] Points)<br>
+	<b>Antagonist contracts completed:</b> [mind?.contracts_completed] ([to_score_color(contracts_score)] Points)<br>
 	<b>Survive:</b> [client.survive ? "Yes" : "No"] ([to_score_color(survive_score)] Points)<br>
 	<b>Escape:</b> [client.escaped ? "Yes" : "No"] ([to_score_color(scaped_score)] Points)<br>
 	<b>Final personal score:</b> [get_color_score(final_score, final_score, max_personal_score)] Points<br><br>
diff --git a/code/game/machinery/doors/airlock.dm b/code/game/machinery/doors/airlock.dm
index 35cf269709a..acbf39b611f 100644
--- a/code/game/machinery/doors/airlock.dm
+++ b/code/game/machinery/doors/airlock.dm
@@ -1273,7 +1273,7 @@ There are 9 wires.
 	SetStunned(5)
 	SetWeakened(5)
 	var/turf/T = get_turf(src)
-	T.add_blood(src)
+	T?.add_blood(src)
 
 /mob/living/carbon/airlock_crush(crush_damage)
 	. = ..()
diff --git a/code/game/objects/items/devices/organ_module/simple.dm b/code/game/objects/items/devices/organ_module/simple.dm
index 33bb2938b97..e1989ba4b6a 100644
--- a/code/game/objects/items/devices/organ_module/simple.dm
+++ b/code/game/objects/items/devices/organ_module/simple.dm
@@ -26,7 +26,7 @@
 
 /obj/item/organ_module/active/simple/proc/retract(obj/item/organ/external/E)
 	if(holding == null)
-		var/mob/M = holding.loc
+		var/mob/M = holding?.loc
 		to_chat(M, SPAN_WARNING("There are nothing to retract"))
 		return
 
diff --git a/code/game/objects/items/devices/transfer_valve.dm b/code/game/objects/items/devices/transfer_valve.dm
index 59ce9fab1bc..c215328e637 100644
--- a/code/game/objects/items/devices/transfer_valve.dm
+++ b/code/game/objects/items/devices/transfer_valve.dm
@@ -11,8 +11,11 @@
 	var/toggle = 1
 	flags = PROXMOVE
 
-/obj/item/device/transfer_valve/proc/process_activation(var/obj/item/device/D)
+	// #TODO-ISKHOD: Probably add the assembly holders to this, as referenced on line 42, which will properly solve the runtime
+	var/obj/item/device/assembly/left_assembly = null
+	var/obj/item/device/assembly/right_assembly = null
 
+/obj/item/device/transfer_valve/proc/process_activation(var/obj/item/device/D)
 
 /obj/item/device/transfer_valve/attackby(obj/item/item, mob/user)
 	var/turf/location = get_turf(src) // For admin logs
diff --git a/code/modules/client/preference_setup/preference_setup.dm b/code/modules/client/preference_setup/preference_setup.dm
index 4886eb86794..6d409f0dd4f 100755
--- a/code/modules/client/preference_setup/preference_setup.dm
+++ b/code/modules/client/preference_setup/preference_setup.dm
@@ -128,7 +128,7 @@ var/const/CHARACTER_PREFERENCE_INPUT_TITLE = "Character Preference"
 		. = 1
 
 	if(.)
-		user.client.prefs.ShowChoices(user)
+		user.client?.prefs.ShowChoices(user)
 
 /**************************
 * Category Category Setup *
diff --git a/code/modules/core_implant/cruciform/rituals/construction.dm b/code/modules/core_implant/cruciform/rituals/construction.dm
index 7c3e2e174a4..1c1d9980565 100644
--- a/code/modules/core_implant/cruciform/rituals/construction.dm
+++ b/code/modules/core_implant/cruciform/rituals/construction.dm
@@ -49,7 +49,7 @@ GLOBAL_LIST_INIT(nt_constructs, init_nt_constructs())
 		if(!ispath(placeholder))
 			continue
 		listed_components += list("[blueprint.materials[placeholder]] [initial(placeholder.name)]")
-	to_chat(user, SPAN_NOTICE("[blueprint.name] requires: [english_list(listed_components)]."))
+	to_chat(user, SPAN_NOTICE("[blueprint?.name] requires: [english_list(listed_components)]."))
 	return TRUE
 
 /datum/ritual/cruciform/priest/construction
diff --git a/code/modules/hivemind/self_defense.dm b/code/modules/hivemind/self_defense.dm
index 300338b9730..cb76ef90200 100644
--- a/code/modules/hivemind/self_defense.dm
+++ b/code/modules/hivemind/self_defense.dm
@@ -136,7 +136,8 @@
 		champion = new champion_path(spawn_loc)
 		champion.maxHealth += 150
 		champion.health += 150
-		champion.malfunction_chance = 0
+		if("malfunction_chance" in champion)
+			champion.malfunction_chance = 0
 		champion.name = "Champion "
 		if(i > champ_names.len)
 			champion.name = champion.name + " " + champ_names[champ_names.len]
diff --git a/code/modules/mob/living/bot/farmbot.dm b/code/modules/mob/living/bot/farmbot.dm
index 78117fd07a0..af0e996a453 100644
--- a/code/modules/mob/living/bot/farmbot.dm
+++ b/code/modules/mob/living/bot/farmbot.dm
@@ -134,11 +134,12 @@
 			if(path.len && frustration < 5)
 				if(path[1] == loc)
 					path -= path[1]
-				var/t = step_towards(src, path[1])
-				if(t)
-					path -= path[1]
-				else
-					++frustration
+				if(path.len > 0 && frustration < 5) // Hopefully fixes list index out of bounds error - Ryuu
+					var/t = step_towards(src, path[1])
+					if(t)
+						path -= path[1]
+					else
+						++frustration
 			else
 				path = list()
 				target = null
diff --git a/code/modules/mob/living/carbon/superior_animal/giant_spider/types/hunter/pepper.dm b/code/modules/mob/living/carbon/superior_animal/giant_spider/types/hunter/pepper.dm
index 256886fec3e..e7c412f09f9 100644
--- a/code/modules/mob/living/carbon/superior_animal/giant_spider/types/hunter/pepper.dm
+++ b/code/modules/mob/living/carbon/superior_animal/giant_spider/types/hunter/pepper.dm
@@ -42,7 +42,7 @@
 /mob/living/carbon/superior/spider/hunter/pepper/Life()
 	. = ..()
 
-	gas_sac.add_reagent("condensedcapsaicinspider", 1)
+	gas_sac?.add_reagent("condensedcapsaicinspider", 1)
 
 /mob/living/carbon/superior/spider/hunter/pepper/handle_attacking_stance(atom/targetted_mob, already_destroying_surroundings)
 	. = ..()
diff --git a/code/modules/mob/living/carbon/superior_animal/superior_animal.dm b/code/modules/mob/living/carbon/superior_animal/superior_animal.dm
index 07f39a604f9..92d7cb5e2bf 100755
--- a/code/modules/mob/living/carbon/superior_animal/superior_animal.dm
+++ b/code/modules/mob/living/carbon/superior_animal/superior_animal.dm
@@ -158,7 +158,7 @@
 							return possible_location // this is a valid target
 						else
 							continue
-				var/index = possible_locations.len
+				var/index = possible_locations?.len
 				return possible_locations[index] //return the last entry in the list
 
 	for (var/turf/possible_location as anything in possible_locations) // iterate through each turf we are considering
diff --git a/code/modules/mob/living/simple_animal/hostile/carp.dm b/code/modules/mob/living/simple_animal/hostile/carp.dm
index c247938429e..792b3bd2488 100644
--- a/code/modules/mob/living/simple_animal/hostile/carp.dm
+++ b/code/modules/mob/living/simple_animal/hostile/carp.dm
@@ -153,6 +153,6 @@
 				return
 		if(H && H.has_shield()) //Having a shield to block the knockdown!
 			L.visible_message(SPAN_DANGER("\the [src] tried to knocks down \the [L]! But [L] blocks \the [src] attack!"))
-		if(prob(knockdown_odds - L.stats.getStat(STAT_TGH))) // Each point of toughness decreases knockdown chance by one
+		if(prob(knockdown_odds - L.stats?.getStat(STAT_TGH))) // Each point of toughness decreases knockdown chance by one
 			L?.Weaken(3)
 			L?.visible_message(SPAN_DANGER("\the [src] knocks down \the [L]!"))
diff --git a/code/modules/mob/living/simple_animal/simple_animal.dm b/code/modules/mob/living/simple_animal/simple_animal.dm
index 9f3ed944f57..b3a78de1bfb 100644
--- a/code/modules/mob/living/simple_animal/simple_animal.dm
+++ b/code/modules/mob/living/simple_animal/simple_animal.dm
@@ -217,7 +217,7 @@
 			to_chat(user, SPAN_DANGER("It looks starving!"))
 		else if (nutrition < max_nutrition *0.5)
 			to_chat(user, SPAN_NOTICE("It looks hungry."))
-		else if ((reagents.total_volume > 0 && nutrition > max_nutrition *0.75) || nutrition > max_nutrition *0.9)
+		else if ((reagents?.total_volume > 0 && nutrition > max_nutrition *0.75) || nutrition > max_nutrition *0.9)
 			to_chat(user, "It looks full and contented.")
 	if (health < maxHealth * 0.10)
 		to_chat(user, SPAN_DANGER("It looks like they are on their last legs!"))
diff --git a/code/modules/mob/mob.dm b/code/modules/mob/mob.dm
index 4d61e696410..6f8c3cb3d2e 100644
--- a/code/modules/mob/mob.dm
+++ b/code/modules/mob/mob.dm
@@ -188,7 +188,7 @@
 				. += TF.flooring.tally_addition_decl
 		. += T.tally_addition
 
-	. += move_intent.move_delay
+	. += move_intent?.move_delay
 
 	if(client)
 		if(client.true_dir == NORTHEAST || client.true_dir == NORTHWEST || client.true_dir == SOUTHEAST || client.true_dir == SOUTHWEST)
diff --git a/code/modules/organs/wound.dm b/code/modules/organs/wound.dm
index 452d3abaf94..bca12b571c0 100644
--- a/code/modules/organs/wound.dm
+++ b/code/modules/organs/wound.dm
@@ -161,7 +161,7 @@
 		amount -= healed_damage
 		src.damage -= healed_damage
 
-		while(src.wound_damage() < damage_list[current_stage] && current_stage < src.desc_list.len)
+		while(current_stage < src.desc_list.len && src.wound_damage() < damage_list[current_stage])
 			current_stage++
 		desc = desc_list[current_stage]
 		src.min_damage = damage_list[current_stage]
diff --git a/code/modules/power/turbine.dm b/code/modules/power/turbine.dm
index 0d6ca62769a..917a09e2fde 100644
--- a/code/modules/power/turbine.dm
+++ b/code/modules/power/turbine.dm
@@ -118,7 +118,7 @@
 #define TURBGENG 0.8
 
 /obj/machinery/power/turbine/Process()
-	if(!compressor.starter)
+	if(!compressor?.starter)
 		return
 	cut_overlays()
 	if(stat & BROKEN)
@@ -276,4 +276,4 @@
 
 /obj/machinery/computer/turbine_computer/Process()
 	src.updateDialog()
-	return
\ No newline at end of file
+	return
diff --git a/code/modules/projectiles/gun.dm b/code/modules/projectiles/gun.dm
index d1c2f27941d..10188f095ec 100644
--- a/code/modules/projectiles/gun.dm
+++ b/code/modules/projectiles/gun.dm
@@ -903,7 +903,10 @@ For the sake of consistency, I suggest always rounding up on even values when ap
 /obj/item/gun/proc/toggle_scope(mob/living/user, switchzoom = FALSE)
 	//looking through a scope limits your periphereal vision
 	//still, increase the view size by a tiny amount so that sniping isn't too restricted to NSEW
-	if(!zoom_factors)
+	if(!zoom_factors || zoom_factors.len > 0)
+		zoom = FALSE
+		return
+	if(active_zoom_factor > 0 || active_zoom_factor > zoom_factors.len)
 		zoom = FALSE
 		return
 	var/tozoom = zoom_factors[active_zoom_factor]
diff --git a/code/modules/reagents/reagents.dm b/code/modules/reagents/reagents.dm
index d20b15b429d..3403bf7d6aa 100755
--- a/code/modules/reagents/reagents.dm
+++ b/code/modules/reagents/reagents.dm
@@ -265,16 +265,19 @@
 /datum/reagent/proc/addiction_act_stage3(mob/living/carbon/human/M)
 	if(prob(30))
 		to_chat(M, SPAN_DANGER("You have an intense craving for [name]."))
-		M.sanity.changeLevel(-5)
+		if(istype(M, /mob/living/carbon/human) && M.sanity)
+			M.sanity.changeLevel(-5)
 
 /datum/reagent/proc/addiction_act_stage4(mob/living/carbon/human/M)
 	if(prob(30))
 		to_chat(M, SPAN_DANGER("You're not feeling good at all! You really need some [name]."))
-		M.sanity.changeLevel(-10)
+		if(istype(M, /mob/living/carbon/human) && M.sanity)
+			M.sanity.changeLevel(-10)
 
 /datum/reagent/proc/addiction_end(mob/living/carbon/human/M)
 	to_chat(M, SPAN_NOTICE("You feel like you've gotten over your need for [name]."))
-	M.sanity.changeLevel(15)
+	if(istype(M, /mob/living/carbon/human) && M.sanity)
+		M.sanity.changeLevel(15)
 
 // Withdrawal
 /datum/reagent/proc/withdrawal_start(mob/living/carbon/M)
@@ -286,7 +289,6 @@
 /datum/reagent/proc/withdrawal_end(mob/living/carbon/M)
 	return
 
-
 /datum/reagent/Destroy() // This should only be called by the holder, so it's already handled clearing its references
 	. = ..()
 	holder = null
diff --git a/code/modules/synthesized_instruments/song.dm b/code/modules/synthesized_instruments/song.dm
index bcafa30d43b..64f80191028 100644
--- a/code/modules/synthesized_instruments/song.dm
+++ b/code/modules/synthesized_instruments/song.dm
@@ -108,7 +108,7 @@
 			current_volume = new_volume
 			continue
 		current_volume = sanitized_volume
-		src.player.event_manager.push_event(src.player, token, tick, current_volume)
+		src.player.event_manager?.push_event(src.player, token, tick, current_volume)
 		if (current_volume <= 0)
 			break
 
@@ -174,7 +174,7 @@
 					cur_octaves[note_off] = octave
 					cur_accidentals[note_off] = accidental
 					play_synthesized_note(note_off, accidental, octave+transposition, duration, src.current_line, cur_note)
-					if (src.player.event_manager.is_overloaded())
+					if (src.player.event_manager?.is_overloaded())
 						STOP_PLAY_LINES
 			cur_note++
 			src.player.event_manager.suspended = 0
diff --git a/code/modules/tgui/tgui.dm b/code/modules/tgui/tgui.dm
index 7cb7648a9ad..4999f5496f9 100644
--- a/code/modules/tgui/tgui.dm
+++ b/code/modules/tgui/tgui.dm
@@ -300,9 +300,9 @@ GLOBAL_LIST_INIT(TGUI_THEMES, list(
 			"key" = window_key,
 			"size" = window_size,
 			"fancy" = user.client?.get_preference_value(/datum/client_preference/tgui_fancy) == GLOB.PREF_YES,
-			"locked" = user.client.get_preference_value(/datum/client_preference/tgui_lock) == GLOB.PREF_YES,
+			"locked" = user.client?.get_preference_value(/datum/client_preference/tgui_lock) == GLOB.PREF_YES,
 			// for bad PCs
-			"toaster" = user.client.get_preference_value(/datum/client_preference/tgui_toaster) == GLOB.PREF_YES,
+			"toaster" = user.client?.get_preference_value(/datum/client_preference/tgui_toaster) == GLOB.PREF_YES,
 			"default_theme" = get_theme_by_pref(user.client.prefs.TGUI_theme)
 		),
 		"client" = list(
diff --git a/code/modules/turbolift/turbolift_console.dm b/code/modules/turbolift/turbolift_console.dm
index 824c0da6102..2c504eb44b7 100644
--- a/code/modules/turbolift/turbolift_console.dm
+++ b/code/modules/turbolift/turbolift_console.dm
@@ -44,7 +44,7 @@
 	return interact(user)
 
 /obj/structure/lift/interact(var/mob/user)
-	if(!lift.is_functional())
+	if(!lift?.is_functional())
 		return 0
 	return 1
 // End base.

```
</details>
