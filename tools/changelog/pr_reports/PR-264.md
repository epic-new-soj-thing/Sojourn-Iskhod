# PR #264: discord linking fixes

- **Author:** AMcilraith
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/264

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

<!-- Describe The Pull Request. -->

<hr>
</details>

## Changelog
```
:cl:
add: Added requirement for Discord account linking to join the game.  
fix: Fixed notifications for users who have not linked their Discord accounts.
/:cl:
```

**Changelog entries (included in build):**

- **fix:** Fixed notifications for users who have not linked their Discord accounts.

<details>
<summary>Files changed</summary>

- `code/controllers/configuration.dm` (+4 / -0) modified
- `code/modules/mob/living/silicon/robot/drone/drone_manufacturer.dm` (+1 / -1) modified
- `code/modules/mob/new_player/new_player.dm` (+3 / -3) modified
- `code/modules/mob/observer/ghost/ghost.dm` (+2 / -2) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/controllers/configuration.dm b/code/controllers/configuration.dm
index 34e5ffbd9cb..1c9d29f56c9 100755
--- a/code/controllers/configuration.dm
+++ b/code/controllers/configuration.dm
@@ -27,6 +27,7 @@ GLOBAL_LIST_EMPTY(storyteller_cache)
 	var/log_runtime = 0					// logs world.log to a file
 	var/log_world_output = 0			// log log_world(messages)
 	var/sql_enabled = 1					// for sql switching
+	var/require_discord_linking = 1		// require Discord linking to join the game
 	var/allow_admin_ooccolor = 0		// Allows admins with relevant permissions to have their own ooc color
 	var/allow_vote_restart = 0 			// allow votes to e
 	var/automatic_restart_time = 0		// server will begin ending the round at this time
@@ -324,6 +325,9 @@ GLOBAL_LIST_EMPTY(storyteller_cache)
 				if ("sql_enabled")
 					config.sql_enabled = text2num(value)
 
+				if ("require_discord_linking")
+					config.require_discord_linking = text2num(value)
+
 				if ("log_say")
 					config.log_say = 1
 
diff --git a/code/modules/mob/living/silicon/robot/drone/drone_manufacturer.dm b/code/modules/mob/living/silicon/robot/drone/drone_manufacturer.dm
index ba2d2616779..e36ec723e1a 100644
--- a/code/modules/mob/living/silicon/robot/drone/drone_manufacturer.dm
+++ b/code/modules/mob/living/silicon/robot/drone/drone_manufacturer.dm
@@ -110,7 +110,7 @@
 			    but in order to play, you'll need to be whitelisted!  Please visit our discord to submit an access request!")
 		return
 
-	if(!BC_IsDiscordLinked(user.ckey) && !usr.client.holder)
+	if(config.require_discord_linking && !BC_IsDiscordLinked(user.ckey) && !usr.client.holder)
 		user << SPAN_DANGER("Discord Account Linking Required! You must link your Discord account before joining. \
 			   Use the /link command in our Discord server or the 'Link Discord to CKey' verb in the OOC tab.")
 		return
diff --git a/code/modules/mob/new_player/new_player.dm b/code/modules/mob/new_player/new_player.dm
index 56852f09c1d..499bb5ec0be 100755
--- a/code/modules/mob/new_player/new_player.dm
+++ b/code/modules/mob/new_player/new_player.dm
@@ -140,7 +140,7 @@
 				alert("Border Control is enabled, and you haven't been whitelisted!  You're welcome to observe, \
 					   but in order to play, you'll need to be whitelisted!  Please visit our discord to submit an access request!" , "Border Control Active")
 				ready = 0
-			else if(!BC_IsDiscordLinked(ckey) && !usr.client.holder)
+			else if(config.require_discord_linking && !BC_IsDiscordLinked(ckey) && !usr.client.holder)
 				alert("Discord Account Linking Required! You must link your Discord account before joining. \
 					   Use the /link command in our Discord server or the 'Link Discord to CKey' verb in the OOC tab.", "Discord Linking Required")
 				ready = 0
@@ -233,7 +233,7 @@
 				   but in order to play, you'll need to be whitelisted!  Please visit our discord to submit an access request!" , "Border Control Active")
 			return 0
 
-		if(!BC_IsDiscordLinked(ckey) && !usr.client.holder)
+		if(config.require_discord_linking && !BC_IsDiscordLinked(ckey) && !usr.client.holder)
 			alert("Discord Account Linking Required! You must link your Discord account before joining. \
 				   Use the /link command in our Discord server or the 'Link Discord to CKey' verb in the OOC tab.", "Discord Linking Required")
 			return 0
@@ -311,7 +311,7 @@
 			but in order to play, you'll need to be whitelisted!  Please visit our discord to submit an access request!" , "Border Control Active")
 		return FALSE
 
-	if(!BC_IsDiscordLinked(ckey) && !usr.client.holder)
+	if(config.require_discord_linking && !BC_IsDiscordLinked(ckey) && !usr.client.holder)
 		alert("Discord Account Linking Required! You must link your Discord account before joining. \
 			   Use the /link command in our Discord server or the 'Link Discord to CKey' verb in the OOC tab.", "Discord Linking Required")
 		return FALSE
diff --git a/code/modules/mob/observer/ghost/ghost.dm b/code/modules/mob/observer/ghost/ghost.dm
index c400666e5e2..bea5ca5a3df 100644
--- a/code/modules/mob/observer/ghost/ghost.dm
+++ b/code/modules/mob/observer/ghost/ghost.dm
@@ -446,7 +446,7 @@ This is the proc mobs get to turn into a ghost. Forked from ghostize due to comp
 			    but in order to play, you'll need to be whitelisted!  Please visit our discord to submit an access request!")
 		return
 
-	if(!BC_IsDiscordLinked(usr.ckey) && !usr.client.holder)
+	if(config.require_discord_linking && !BC_IsDiscordLinked(usr.ckey) && !usr.client.holder)
 		usr << SPAN_DANGER("Discord Account Linking Required! You must link your Discord account before joining. \
 			   Use the /link command in our Discord server or the 'Link Discord to CKey' verb in the OOC tab.")
 		return
@@ -592,7 +592,7 @@ This is the proc mobs get to turn into a ghost. Forked from ghostize due to comp
 				but in order to play, you'll need to be whitelisted!  Please visit our discord to submit an access request!"))
 		return 0
 
-	if(!BC_IsDiscordLinked(usr.ckey) && !usr.client.holder)
+	if(config.require_discord_linking && !BC_IsDiscordLinked(usr.ckey) && !usr.client.holder)
 		to_chat(usr, SPAN_DANGER("Discord Account Linking Required! You must link your Discord account before joining. \
 			   Use the /link command in our Discord server or the 'Link Discord to CKey' verb in the OOC tab."))
 		return 0

```
</details>
