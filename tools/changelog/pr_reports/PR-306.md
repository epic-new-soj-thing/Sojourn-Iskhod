# PR #306: fixes sum compile errors

- **Author:** AMcilraith
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/306

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

<!-- Describe The Pull Request. -->

<hr>
</details>

## Changelog
```
:cl:
fix: Fixed compilation errors in firedoors and atmospherics.
/:cl:
```

**Changelog entries (included in build):**

- **fix:** Fixed compilation errors in firedoors and atmospherics.

<details>
<summary>Files changed</summary>

- `code/game/machinery/doors/firedoor.dm` (+3 / -3) modified
- `code/modules/admin/verbs/atmosdebug.dm` (+8 / -3) modified
- `tgui/packages/tgui/interfaces/Changelog.tsx` (+9 / -11) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/game/machinery/doors/firedoor.dm b/code/game/machinery/doors/firedoor.dm
index e0576ced416..2cffdc64d52 100644
--- a/code/game/machinery/doors/firedoor.dm
+++ b/code/game/machinery/doors/firedoor.dm
@@ -12,9 +12,9 @@
 #define F_SOUTH "South"
 #define F_EAST "East"
 #define F_WEST "West"
-#define FIREDOOR_TURF 1
-#define FIREDOOR_ATMOS 2
-#define FIREDOOR_ALERT 3
+#define FIREDOOR_TURF "1"
+#define FIREDOOR_ATMOS "2"
+#define FIREDOOR_ALERT "3"
 /obj/machinery/door/firedoor
 	name = "\improper Emergency Shutter"
 	desc = "Emergency air-tight shutter, capable of sealing off breached areas."
diff --git a/code/modules/admin/verbs/atmosdebug.dm b/code/modules/admin/verbs/atmosdebug.dm
index 2d561c5c2b2..a56488468cf 100644
--- a/code/modules/admin/verbs/atmosdebug.dm
+++ b/code/modules/admin/verbs/atmosdebug.dm
@@ -29,12 +29,17 @@
 	next_turf:
 		for(var/turf/T in turfs)
 			for(var/dir in cardinal)
-				var/list/connect_types = list(1 = 0, 2 = 0, 3 = 0)
+				var/list/connect_types = list("1" = 0, "2" = 0, "3" = 0)
 				for(var/obj/machinery/atmospherics/pipe in T)
 					if(dir & pipe.initialize_directions)
 						for(var/connect_type in pipe.connect_types)
-							connect_types[connect_type] += 1
-						if(connect_types[1] > 1 || connect_types[2] > 1 || connect_types[3] > 1)
+						if(connect_types["[connect_type]"] > 1) // connect_type is likely 1, 2 or 3.
+						// actually connect_type is 1,2,3 from pipe.connect_types.
+						// The previous code was: connect_types[connect_type] += 1
+						// connect_type is likely integer.
+						// So we need to access string key.
+						connect_types["[connect_type]"] += 1
+						if(connect_types["1"] > 1 || connect_types["2"] > 1 || connect_types["3"] > 1)
 							to_chat(usr, "Overlapping pipe ([pipe.name]) located at [T.x],[T.y],[T.z] ([get_area(T)])")
 							continue next_turf
 	to_chat(usr, "Done")
diff --git a/tgui/packages/tgui/interfaces/Changelog.tsx b/tgui/packages/tgui/interfaces/Changelog.tsx
index b5a74e19f63..85249af774d 100644
--- a/tgui/packages/tgui/interfaces/Changelog.tsx
+++ b/tgui/packages/tgui/interfaces/Changelog.tsx
@@ -29,8 +29,6 @@ const icons = {
   imagedel: { icon: 'tg-image-minus', color: 'red' },
   qol: { icon: 'hand-holding-heart', color: 'green' },
   refactor: { icon: 'tools', color: 'green' },
-  rscadd: { icon: 'check-circle', color: 'green' },
-  rscdel: { icon: 'times-circle', color: 'red' },
   add: { icon: 'check-circle', color: 'green' },
   del: { icon: 'times-circle', color: 'red' },
   server: { icon: 'server', color: 'purple' },
@@ -46,12 +44,12 @@ const icons = {
 
 type ChangelogState = {
   data:
-    | string
-    | {
-        [date: string]: {
-          [author: string]: string[];
-        };
-      };
+  | string
+  | {
+    [date: string]: {
+      [author: string]: string[];
+    };
+  };
   selectedDate: any;
   selectedIndex: any;
 };
@@ -152,7 +150,7 @@ export class Changelog extends Component<{}, ChangelogState> {
               window.scrollTo(
                 0,
                 document.body.scrollHeight ||
-                  document.documentElement.scrollHeight,
+                document.documentElement.scrollHeight,
               );
               return this.getData(dates[index]);
             }}
@@ -171,7 +169,7 @@ export class Changelog extends Component<{}, ChangelogState> {
               window.scrollTo(
                 0,
                 document.body.scrollHeight ||
-                  document.documentElement.scrollHeight,
+                document.documentElement.scrollHeight,
               );
               return this.getData(dates[index]);
             }}
@@ -193,7 +191,7 @@ export class Changelog extends Component<{}, ChangelogState> {
               window.scrollTo(
                 0,
                 document.body.scrollHeight ||
-                  document.documentElement.scrollHeight,
+                document.documentElement.scrollHeight,
               );
               return this.getData(dates[index]);
             }}

```
</details>
