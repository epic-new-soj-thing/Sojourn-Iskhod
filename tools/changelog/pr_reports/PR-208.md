# PR #208: Chem master plus

- **Author:** AMcilraith
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/208

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

<!-- Describe The Pull Request. -->

<hr>
</details>

## Changelog
```
:cl:
add: Added tier-3 default components for the chem dispenser when no parts are placed.  
tweak: Chem dispenser now replaces default parts with tier-3 components if all parts are stock parts or cells.  
fix: Fixed issue where components might duplicate when initializing the chem dispenser.
/:cl:
```

**Changelog entries (included in build):**

- **tweak:** Chem dispenser now replaces default parts with tier-3 components if all parts are stock parts or cells.
- **fix:** Fixed issue where components might duplicate when initializing the chem dispenser.

<details>
<summary>Files changed</summary>

- `.github/workflows/ci_suite.yml` (+24 / -10) modified
- `code/modules/reagents/machinery/chem_dispenser.dm` (+25 / -3) modified
- `tools/ci/download_byond.sh` (+7 / -1) modified
- `tools/ci/install_byond.sh` (+15 / -1) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/.github/workflows/ci_suite.yml b/.github/workflows/ci_suite.yml
index dce923b6474..57f5849858b 100644
--- a/.github/workflows/ci_suite.yml
+++ b/.github/workflows/ci_suite.yml
@@ -71,15 +71,22 @@ jobs:
       - name: Compile All Maps
         run: |
           bash tools/ci/install_byond.sh
-          # Verify BYOND zip download
+          # Debug BYOND zip download
+          echo "Listing byond.zip:"
+          ls -lh byond.zip || true
+          echo "Checking byond.zip size:"
+          stat byond.zip || true
+          echo "Checking byond.zip checksum:"
+          sha256sum byond.zip || true
+          # Fail if file is missing or too small
           if [ ! -f byond.zip ]; then
             echo "byond.zip not found! Download failed."
             exit 1
           fi
-          echo "byond.zip size:"
-          ls -lh byond.zip
-          echo "byond.zip checksum:"
-          sha256sum byond.zip
+          if [ $(stat -c%s "byond.zip") -lt 10000 ]; then
+            echo "byond.zip is too small, likely a failed download."
+            exit 1
+          fi
           unzip -l byond.zip || { echo "byond.zip is not a valid zip file!"; exit 1; }
           source $HOME/BYOND/byond/bin/byondsetup
           tools/build/build --ci dm -DCIBUILDING -DCITESTING -DALL_MAPS
@@ -120,15 +127,22 @@ jobs:
       - name: Compile Tests
         run: |
           bash tools/ci/install_byond.sh
-          # Verify BYOND zip download
+          # Debug BYOND zip download
+          echo "Listing byond.zip:"
+          ls -lh byond.zip || true
+          echo "Checking byond.zip size:"
+          stat byond.zip || true
+          echo "Checking byond.zip checksum:"
+          sha256sum byond.zip || true
+          # Fail if file is missing or too small
           if [ ! -f byond.zip ]; then
             echo "byond.zip not found! Download failed."
             exit 1
           fi
-          echo "byond.zip size:"
-          ls -lh byond.zip
-          echo "byond.zip checksum:"
-          sha256sum byond.zip
+          if [ $(stat -c%s "byond.zip") -lt 10000 ]; then
+            echo "byond.zip is too small, likely a failed download."
+            exit 1
+          fi
           unzip -l byond.zip || { echo "byond.zip is not a valid zip file!"; exit 1; }
           source $HOME/BYOND/byond/bin/byondsetup
           tools/build/build --ci dm -DCIBUILDING -DANSICOLORS
diff --git a/code/modules/reagents/machinery/chem_dispenser.dm b/code/modules/reagents/machinery/chem_dispenser.dm
index 8e2ede7555a..894aefb7830 100644
--- a/code/modules/reagents/machinery/chem_dispenser.dm
+++ b/code/modules/reagents/machinery/chem_dispenser.dm
@@ -473,12 +473,34 @@
 		// Call parent Initialize() so the dispenser's base initialization runs.
 		..()
 
-		// If no parts were placed on the machine (map or builder), pre-fill with tier-3 components.
-		if(!component_parts || !length(component_parts))
+		// If parts were placed by the map as default components, replace them with tier-3 parts.
+		// Detection heuristic: treat existing parts as "default" if every component is a stock_part or a cell.
+		if(component_parts && length(component_parts))
+			var/all_default = TRUE
+			for(var/obj/item/I in component_parts)
+				if(!I) continue
+				if(!(istype(I, /obj/item/stock_parts) || istype(I, /obj/item/cell)))
+					all_default = FALSE
+					break
+
+			if(all_default)
+				// Remove the map-spawned/default parts to avoid duplicates
+				for(var/obj/item/I2 in component_parts)
+					if(I2)
+						qdel(I2)
+
+				component_parts = list()
+				component_parts += new /obj/item/cell/large/moebius/omega(null)
+				component_parts += new /obj/item/stock_parts/capacitor/super(null)
+				component_parts += new /obj/item/stock_parts/matter_bin/super(null)
+				component_parts += new /obj/item/stock_parts/manipulator/pico(null)
+
+		// If there were no parts at all, pre-fill with tier-3 components (same as replacement behavior).
+		else
+			component_parts = list()
 			component_parts += new /obj/item/cell/large/moebius/omega(null)
 			component_parts += new /obj/item/stock_parts/capacitor/super(null)
 			component_parts += new /obj/item/stock_parts/matter_bin/super(null)
-			component_parts += new /obj/item/stock_parts/matter_bin/super(null)
 			component_parts += new /obj/item/stock_parts/manipulator/pico(null)
 
 		// Recalculate ratings and ensure UI/icon reflect parts
diff --git a/tools/ci/download_byond.sh b/tools/ci/download_byond.sh
index 19b7f201707..5445a657e79 100755
--- a/tools/ci/download_byond.sh
+++ b/tools/ci/download_byond.sh
@@ -2,4 +2,10 @@
 set -e
 source dependencies.sh
 echo "Downloading BYOND version $BYOND_MAJOR.$BYOND_MINOR"
-curl "http://www.byond.com/download/build/$BYOND_MAJOR/$BYOND_MAJOR.${BYOND_MINOR}_byond.zip" -o C:/byond.zip
+curl -fSL "http://www.byond.com/download/build/$BYOND_MAJOR/$BYOND_MAJOR.${BYOND_MINOR}_byond.zip" -o byond.zip
+if [ $? -ne 0 ]; then
+  echo "BYOND download failed!"
+  exit 1
+fi
+echo "BYOND zip file details:"
+ls -lh byond.zip
diff --git a/tools/ci/install_byond.sh b/tools/ci/install_byond.sh
index 4a688755d3d..7b6f2100e65 100755
--- a/tools/ci/install_byond.sh
+++ b/tools/ci/install_byond.sh
@@ -11,7 +11,21 @@ else
   rm -rf "$HOME/BYOND"
   mkdir -p "$HOME/BYOND"
   cd "$HOME/BYOND"
-  curl "http://www.byond.com/download/build/${BYOND_MAJOR}/${BYOND_MAJOR}.${BYOND_MINOR}_byond_linux.zip" -o byond.zip
+  echo "Downloading BYOND from: http://www.byond.com/download/build/${BYOND_MAJOR}/${BYOND_MAJOR}.${BYOND_MINOR}_byond_linux.zip"
+  curl -fSL "http://www.byond.com/download/build/${BYOND_MAJOR}/${BYOND_MAJOR}.${BYOND_MINOR}_byond_linux.zip" -o byond.zip
+  if [ ! -f byond.zip ]; then
+    echo "byond.zip not found! Download failed."
+    exit 1
+  fi
+  echo "byond.zip size:"
+  ls -lh byond.zip
+  echo "byond.zip checksum:"
+  sha256sum byond.zip
+  if [ $(stat -c%s "byond.zip") -lt 10000 ]; then
+    echo "byond.zip is too small, likely a failed download."
+    exit 1
+  fi
+  unzip -l byond.zip || { echo "byond.zip is not a valid zip file!"; exit 1; }
   unzip byond.zip
   rm byond.zip
   cd byond

```
</details>
