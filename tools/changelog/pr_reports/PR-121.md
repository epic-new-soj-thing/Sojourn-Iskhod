# PR #121: Runtime Fixes

- **Author:** ryuu-dragon
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/121

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

Fixes various runtimes (listed below).
Credit to AlexMorgan3817 for the Music Tape fix (https://github.com/discordia-space/CEV-Eris/pull/6471)
<hr>
</details>

## Changelog
```
:cl:
fix: Fixed issues with music tape song selection when playlists or tracks are empty.  
fix: Corrected power regeneration calculation for core implants when disciples are present.  
fix: Ensured dinosaur targeting correctly checks for job department flags.  
fix: Improved item movement handling for structures to avoid errors when target location is null.  
fix: Fixed potential issues when fetching a body modification that might be absent.  
fix: Prevented errors when handling folder contents to ensure the folder interaction behaves correctly.  
fix: Enhanced projectile handling to avoid errors when accessing attributes of potentially null objects.
/:cl:
```

**Changelog entries (included in build):**

- **fix:** Corrected power regeneration calculation for core implants when disciples are present.
- **fix:** Ensured dinosaur targeting correctly checks for job department flags.
- **fix:** Improved item movement handling for structures to avoid errors when target location is null.
- **fix:** Fixed potential issues when fetching a body modification that might be absent.
- **fix:** Prevented errors when handling folder contents to ensure the folder interaction behaves correctly.
- **fix:** Enhanced projectile handling to avoid errors when accessing attributes of potentially null objects.

<details>
<summary>Files changed</summary>

- `code/game/objects/items/musictapes.dm` (+11 / -8) modified
- `code/modules/core_implant/cruciform/machinery/eotp.dm` (+1 / -1) modified
- `code/modules/mob/animations.dm` (+1 / -1) modified
- `code/modules/mob/living/simple_animal/hostile/dino.dm` (+1 / -1) modified
- `code/modules/multiz/structures.dm` (+2 / -2) modified
- `code/modules/organs/body_modifications.dm` (+1 / -1) modified
- `code/modules/paperwork/folders.dm` (+1 / -1) modified
- `code/modules/projectiles/guns/_projectile.dm` (+3 / -3) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/game/objects/items/musictapes.dm b/code/game/objects/items/musictapes.dm
index f19da56d740..f75eb91e13b 100644
--- a/code/game/objects/items/musictapes.dm
+++ b/code/game/objects/items/musictapes.dm
@@ -12,11 +12,13 @@
 /obj/item/music_tape/New()
 	name = "[name] #[rand(1, 999)]"
 	icon_state = "[rand(1, 15)]"
-	songlist = pick(GLOB.all_playlists)
-	for(var/datum/track/T in GLOB.all_jukebox_tracks)
-		if(T.playlist == songlist)
-			tracklist |= T
-	..()
+	if(length(GLOB.all_playlists))
+		songlist = pick(GLOB.all_playlists)
+	if(length(GLOB.all_jukebox_tracks))
+		for(var/datum/track/T in GLOB.all_jukebox_tracks)
+			if(T.playlist == songlist)
+				tracklist |= T
+	. = ..()
 
 /obj/item/music_tape/examine(mob/user)
 	..()
@@ -34,6 +36,7 @@
 	tracklist = list() //Cleans up the song list
 	songlist = "cringe"
 	icon_state = "5"
-	for(var/datum/track/T in GLOB.all_jukebox_tracks)
-		if(T.secret)
-			tracklist |= T
\ No newline at end of file
+	if(length(GLOB.all_jukebox_tracks))
+		for(var/datum/track/T in GLOB.all_jukebox_tracks)
+			if(T.secret)
+				tracklist |= T
diff --git a/code/modules/core_implant/cruciform/machinery/eotp.dm b/code/modules/core_implant/cruciform/machinery/eotp.dm
index 6a273085975..0f48559fdc5 100644
--- a/code/modules/core_implant/cruciform/machinery/eotp.dm
+++ b/code/modules/core_implant/cruciform/machinery/eotp.dm
@@ -200,7 +200,7 @@ var/global/obj/machinery/power/eotp/eotp
 	else if(type_release == ENERGY_REWARD)
 		for(var/mob/living/carbon/human/H in disciples)
 			var/obj/item/implant/core_implant/cruciform/C = H.get_core_implant(/obj/item/implant/core_implant/cruciform)
-			C.power_regen += initial(C.power_regen)
+			C?.power_regen += initial(C?.power_regen)
 			to_chat(H, SPAN_NOTICE("Your cruciform vibrates, its power regeneration enhancing temporarily."))
 
 	//for(var/disciple in disciples)
diff --git a/code/modules/mob/animations.dm b/code/modules/mob/animations.dm
index 994a91a6f61..6a17aadf87e 100755
--- a/code/modules/mob/animations.dm
+++ b/code/modules/mob/animations.dm
@@ -319,7 +319,7 @@ note dizziness decrements automatically in the mob's Life() proc.
 					M.client.eye = locate(dd_range(1,M.loc.x+rand(-strength,strength),world.maxx),dd_range(1,M.loc.y+rand(-strength,strength),world.maxy),M.loc.z)
 				sleep(1)
 
-		M.client.eye=oldeye
+		M.client?.eye=oldeye
 		M.shakecamera = 0
 
 
diff --git a/code/modules/mob/living/simple_animal/hostile/dino.dm b/code/modules/mob/living/simple_animal/hostile/dino.dm
index c37bac48ffb..a9a76e8d1e1 100644
--- a/code/modules/mob/living/simple_animal/hostile/dino.dm
+++ b/code/modules/mob/living/simple_animal/hostile/dino.dm
@@ -68,7 +68,7 @@
 			var/mob/living/L = A
 			if(istype(L,/mob/living/carbon/human))
 				var/mob/living/carbon/human/thetarget = L
-				if(thetarget.mind?.assigned_job.department_flag == BLACKSHIELD)
+				if(thetarget.mind?.assigned_job?.department_flag == BLACKSHIELD)
 					if(istype(L.lastarea,/area/nadezhda/pros/prep) || istype(L.lastarea,/area/nadezhda/pros/foreman))
 						if(!SA_attackable(thetarget))
 							stance = HOSTILE_STANCE_ATTACK
diff --git a/code/modules/multiz/structures.dm b/code/modules/multiz/structures.dm
index 0dd1a746d8c..b66175c12bc 100644
--- a/code/modules/multiz/structures.dm
+++ b/code/modules/multiz/structures.dm
@@ -121,7 +121,7 @@
 			user.visible_message(SPAN_WARNING("[user] throws [C] [through] \the [src]!"),
 			SPAN_WARNING("You throw [C] [through] \the [src]."))
 			user.drop_item()
-			C.forceMove(target.loc)
+			C.forceMove(target?.loc)
 			var/direction = pick(NORTH, SOUTH, EAST, WEST, NORTHEAST, NORTHWEST, SOUTHEAST, SOUTHWEST)
 			C.Move(get_step(C, direction))
 			if(istype(C, /obj/item/grenade))
@@ -357,4 +357,4 @@
 		free_deepmaint_ladders -= src
 		my_burrow.collapse()
 
-	..()
\ No newline at end of file
+	..()
diff --git a/code/modules/organs/body_modifications.dm b/code/modules/organs/body_modifications.dm
index 75d8064c952..9e35a08fed3 100755
--- a/code/modules/organs/body_modifications.dm
+++ b/code/modules/organs/body_modifications.dm
@@ -58,7 +58,7 @@ var/global/list/modifications_types = list(
 			parent_organ = organ_parent
 
 	if(parent_organ)
-		var/datum/body_modification/parent = P.get_modification(parent_organ)
+		var/datum/body_modification/parent = P?.get_modification(parent_organ)
 		if(parent.nature > nature)
 			to_chat(usr, "[name] can't be attached to [parent.name]")
 			return FALSE
diff --git a/code/modules/paperwork/folders.dm b/code/modules/paperwork/folders.dm
index 656eabcd22a..2bcf9627222 100644
--- a/code/modules/paperwork/folders.dm
+++ b/code/modules/paperwork/folders.dm
@@ -73,7 +73,7 @@
 
 /obj/item/folder/AltClick(mob/user)
 	..()
-	if(contents)
+	if(contents && contents.len > 0)
 		remove_item(contents[1], user)
 
 /obj/item/folder/update_icon()
diff --git a/code/modules/projectiles/guns/_projectile.dm b/code/modules/projectiles/guns/_projectile.dm
index 247895d88c8..df3466d2fb9 100644
--- a/code/modules/projectiles/guns/_projectile.dm
+++ b/code/modules/projectiles/guns/_projectile.dm
@@ -447,17 +447,17 @@
 /obj/item/gun/projectile/get_dud_projectile()
 	var/proj_type
 	if(chambered)
-		proj_type = chambered.BB.type
+		proj_type = chambered.BB?.type
 	else if(loaded.len)
 		var/obj/item/ammo_casing/A = loaded[1]
 		if(!A.BB)
 			return null
-		proj_type = A.BB.type
+		proj_type = A.BB?.type
 	else if(ammo_magazine && ammo_magazine.stored_ammo.len)
 		var/obj/item/ammo_casing/A = ammo_magazine.stored_ammo[1]
 		if(!A.BB)
 			return null
-		proj_type = A.BB.type
+		proj_type = A.BB?.type
 	if(!proj_type)
 		return null
 	return new proj_type

```
</details>
