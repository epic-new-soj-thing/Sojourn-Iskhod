# PR #112: Species Language Adjustments

- **Author:** AMcilraith
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/112

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

Makes a few adjustments to species, and adds a selectable perk restricted to FBPs only to use old languages.
<hr>
</details>

## Changelog
```
:cl:
add: Introduced the 'Lost Tongue' perk allowing certain species to regain knowledge of their native language.  
del: Removed the ability for Crinos to be selected as a known language option for backgrounds.  
tweak: Adjusted language options available for certain perks, including removing Crinos.  
fix: Corrected the language limitations for characters with the 'Lost Tongue' background to only include specific languages.
/:cl:
```

**Changelog entries (included in build):**

- **del:** Removed the ability for Crinos to be selected as a known language option for backgrounds.
- **tweak:** Adjusted language options available for certain perks, including removing Crinos.
- **fix:** Corrected the language limitations for characters with the 'Lost Tongue' background to only include specific languages.

<details>
<summary>Files changed</summary>

- `code/__DEFINES/perks.dm` (+3 / -1) modified
- `code/datums/perks/backgrounds.dm` (+34 / -1) modified
- `code/datums/perks/racial.dm` (+0 / -1) modified
- `code/datums/setup_option/backgrounds/origin_upbringing.dm` (+15 / -0) modified
- `code/modules/mob/language/outsider.dm` (+1 / -1) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/__DEFINES/perks.dm b/code/__DEFINES/perks.dm
index 7a9dfa00031..60e5833fc54 100644
--- a/code/__DEFINES/perks.dm
+++ b/code/__DEFINES/perks.dm
@@ -165,6 +165,8 @@
 #define PERK_SLIMEMETH /datum/perk/racial/speed_boost
 #define PERK_SLIMEBRAIN /datum/perk/racial/slime_stat_boost/mental
 #define PERK_SLIMEBUFF /datum/perk/racial/slime_stat_boost/physical
+//FBPs
+#define PERK_LOST_TONGUE /datum/perk/linguist_fbp
 
 //////////////
 //Core Perks//
@@ -250,4 +252,4 @@
 #define PERK_COOLDOWN_REASON /datum/perk/cooldown/reason
 #define PERK_COOLDOWN_EXERTION /datum/perk/cooldown/exertion
 //Anti-art
-#define PERK_ARTIST_NO /datum/perk/cooldown/artist_no
\ No newline at end of file
+#define PERK_ARTIST_NO /datum/perk/cooldown/artist_no
diff --git a/code/datums/perks/backgrounds.dm b/code/datums/perks/backgrounds.dm
index cd11041370c..9791972ba9c 100644
--- a/code/datums/perks/backgrounds.dm
+++ b/code/datums/perks/backgrounds.dm
@@ -64,7 +64,6 @@ This is NOT for racial-specific perks, but rather specifically for general backg
 	options["Latin"] = LANGUAGE_LATIN
 	options["Akula"] = LANGUAGE_AKULA
 	options["Narad Pidgin"] = LANGUAGE_MERP
-	options["Crinos"] = LANGUAGE_SABLEKYNE
 	var/choice = input(M,"Which language do you know?","Linguist Choice") as null|anything in options
 	if(src && choice)
 		M.add_language(choice)
@@ -421,3 +420,37 @@ This is NOT for racial-specific perks, but rather specifically for general backg
 
 	return ..()
 
+
+///////////////////////////////////// FBP perks
+
+/datum/perk/linguist_fbp
+	name = "Lost Tongue"
+	desc = "You once spoke a language native for your people, and now you must use a synthetic tongue to do so."
+	icon_state = "linguist"
+	active = FALSE
+	passivePerk = FALSE
+	var/anti_cheat = FALSE
+
+/datum/perk/linguist_fbp/activate()
+	..()
+	if(anti_cheat)
+		to_chat(holder, "Recalling your native language is not as easy for someone who was not once an organic.")
+		return FALSE
+	anti_cheat = TRUE
+	var/mob/M = usr
+	var/list/options = list()
+	options["Akula"] = LANGUAGE_AKULA
+	options["Narad Pidgin"] = LANGUAGE_MERP
+	options["Crinos"] = LANGUAGE_SABLEKYNE
+	options["Marqua"] = LANGUAGE_MARQUA
+	options["Kriosan"] = LANGUAGE_KRIOSAN
+	options["Opifexee"] = LANGUAGE_OPIFEXEE
+	var/choice = input(M,"Which language did you once know like the back of your hand?","Original Language Choice") as null|anything in options
+	if(src && choice)
+		M.add_language(choice)
+		M.stats.removePerk(PERK_LOST_TONGUE)
+	anti_cheat = FALSE
+	return TRUE
+
+/datum/perk/linguist_fbp/remove()
+	..()
diff --git a/code/datums/perks/racial.dm b/code/datums/perks/racial.dm
index 8273d4b7df8..5a02cd96601 100644
--- a/code/datums/perks/racial.dm
+++ b/code/datums/perks/racial.dm
@@ -179,7 +179,6 @@
 	options["Latin"] = LANGUAGE_LATIN
 	options["Akula"] = LANGUAGE_AKULA
 	options["Narad Pidgin"] = LANGUAGE_MERP
-	options["Crinos"] = LANGUAGE_SABLEKYNE
 	var/choice = input(M,"Which language do you know?","Linguist Choice") as null|anything in options
 	if(src && choice)
 		M.add_language(choice)
diff --git a/code/datums/setup_option/backgrounds/origin_upbringing.dm b/code/datums/setup_option/backgrounds/origin_upbringing.dm
index 496f6c99b36..00755fb2159 100644
--- a/code/datums/setup_option/backgrounds/origin_upbringing.dm
+++ b/code/datums/setup_option/backgrounds/origin_upbringing.dm
@@ -74,6 +74,21 @@
 		STAT_COG = 0
 	)
 
+/datum/category_item/setup_option/background/bckgrnd/lost_tongue
+	name = "Lost Tongue"
+	desc = "Having once been an alien in an organic body, you now have been confined to a mechanical chassis that feels so unfamiliar. Through special modification, you have been once more granted the ability to speak your native tongue."
+
+	perks = list(PERK_LOST_TONGUE)
+	restricted_to_species = list(FORM_FBP)
+	stat_modifiers = list(
+		STAT_ROB = 0,
+		STAT_TGH = 0,
+		STAT_VIG = 0,
+		STAT_BIO = 0,
+		STAT_MEC = 0,
+		STAT_COG = 0
+	)
+
 /datum/category_item/setup_option/background/bckgrnd/linguist
 	name = "Aspiring Linguist"
 	desc = "Be it from your education or from a multi-lingual family, you've found yourself studying languages non-stop throughout your childhood and early adulthood. Unlike most you're silver-tongued \
diff --git a/code/modules/mob/language/outsider.dm b/code/modules/mob/language/outsider.dm
index baa6c649318..750018aaa20 100644
--- a/code/modules/mob/language/outsider.dm
+++ b/code/modules/mob/language/outsider.dm
@@ -215,7 +215,7 @@
 	speech_verb = list("chatters")
 	ask_verb = list("mrowls")
 	exclaim_verb = list("yelps")
-	flags = NO_SPEAK
+	flags = NO_SPEAK | RESTRICTED
 	colour = "crinos"
 	key = "5"
 	space_chance = 50

```
</details>
