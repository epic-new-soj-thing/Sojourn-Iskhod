# PR #72: 25.05.08 - CFW

- **Author:** CFWMagic
- **Link:** https://github.com/epic-new-soj-thing/Sojourn-Iskhod/pull/72

## About The Pull Request
<details>
<summary>About The Pull Request</summary>
<hr>

Scrambled updates from CFW work branch
<hr>
</details>

## Changelog
```
:cl:
balance: Increased the number of available character slots from 10 to 20.  
tweak: Psionic tools can no longer be used to wedge doors.  
fix: Glass containers now update their name label without playing a sound when specified.
/:cl:
```

**Changelog entries (included in build):**

- **tweak:** Psionic tools can no longer be used to wedge doors.
- **fix:** Glass containers now update their name label without playing a sound when specified.

<details>
<summary>Files changed</summary>

- `code/controllers/configuration.dm` (+1 / -1) modified
- `code/game/machinery/doors/airlock.dm` (+1 / -1) modified
- `code/modules/reagents/reagent_containers/glass.dm` (+4 / -3) modified

</details>

<details>
<summary>Diff</summary>

```diff
diff --git a/code/controllers/configuration.dm b/code/controllers/configuration.dm
index 922bed9e7cb..a4c9b069d60 100755
--- a/code/controllers/configuration.dm
+++ b/code/controllers/configuration.dm
@@ -75,7 +75,7 @@ GLOBAL_LIST_EMPTY(storyteller_cache)
 	var/use_cortical_stacks = 0			//enables neural lace
 	var/empty_server_restart_time = 0	// Time in minutes before empty server will restart
 
-	var/character_slots = 10				// The number of available character slots
+	var/character_slots = 20				// The number of available character slots
 	var/loadout_slots = 3					// The number of loadout slots per character
 
 	var/max_gear_cost = 10 // Used in chargen for accessory loadout limit. 0 disables loadout, negative allows infinite points.
diff --git a/code/game/machinery/doors/airlock.dm b/code/game/machinery/doors/airlock.dm
index d56a7557fcf..35cf269709a 100644
--- a/code/game/machinery/doors/airlock.dm
+++ b/code/game/machinery/doors/airlock.dm
@@ -633,7 +633,7 @@ There are 9 wires.
 		to_chat(usr, SPAN_WARNING("You can't do this."))
 		return
 	var/obj/item/tool/T = usr.get_active_hand()
-	if(istype(T) && T.w_class >= ITEM_SIZE_NORMAL) // We do the checks before proc call, because see "proc overhead".
+	if(istype(T) && T.w_class >= ITEM_SIZE_NORMAL && !istype(T,/obj/item/tool/psionic_omnitool) && !istype(T,/obj/item/tool/knife/psionic_blade)) // We do the checks before proc call, because see "proc overhead".
 		if(istype(T,/obj/item/tool/psionic_omnitool) || istype(T,/obj/item/tool/knife/psionic_blade))
 			to_chat(usr, SPAN_NOTICE("You can't wedge your psionic item in."))
 			return
diff --git a/code/modules/reagents/reagent_containers/glass.dm b/code/modules/reagents/reagent_containers/glass.dm
index 645e24709cf..f6bd8b7dc4a 100644
--- a/code/modules/reagents/reagent_containers/glass.dm
+++ b/code/modules/reagents/reagent_containers/glass.dm
@@ -52,7 +52,7 @@
 	. = ..()
 	base_name = name
 	//For when beakers are pre-labled.
-	update_name_label()
+	update_name_label(FALSE)
 	update_icon()
 
 /obj/item/reagent_containers/glass/proc/has_lid()
@@ -162,8 +162,9 @@
 
 	..()
 
-/obj/item/reagent_containers/glass/proc/update_name_label()
-	playsound(src,'sound/effects/PEN_Ball_Point_Pen_Circling_01_mono.ogg',40,1)
+/obj/item/reagent_containers/glass/proc/update_name_label(play_sound = TRUE)
+	if (play_sound)
+		playsound(src,'sound/effects/PEN_Ball_Point_Pen_Circling_01_mono.ogg',40,1)
 	if(label_text == "")
 		name = base_name
 	else

```
</details>
